#include <iostream>
using namespace std;
int main() {
    float principal, rate, time, CI;
    cout << "Enter principal amount: ";
    cin >> principal;
    cout << "Enter rate of interest: ";
    cin >> rate;
    cout << "Enter time (): ";
    cin >> time;
    CI = principal*(1+R/100)pow(time) - principal;
    cout << "Compound Interest = " << CI << endl ;
    return 0;
    
}